#include <bits/stdc++.h>
using namespace std;

int32_t main()
{
    ios_base::sync_with_stdio(0), cin.tie(0);
    vector<int> v = {2, 3};
    v.reserve(200);
    for (auto val : v)
        cout << val << ' ';
    cout << '\n';
    return 0;
}